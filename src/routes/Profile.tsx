/**
 * Profile Route - User profile and account settings
 *
 * Placeholder for future implementation
 */
export const Profile = () => {
  return (
    <div className="flex h-screen flex-col items-center justify-center bg-background">
      <div className="text-center pb-[calc(3.5rem+env(safe-area-inset-bottom))] md:pb-[calc(4rem+env(safe-area-inset-bottom))]">
        <h1 className="text-2xl font-bold mb-2">Profile</h1>
        <p className="text-muted-foreground">
          Your profile and account settings will appear here.
        </p>
      </div>
    </div>
  )
}
